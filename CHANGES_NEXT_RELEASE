- Use MQTT v5 shared subscriptions to avoid dupplicated messages per agent type (upgrade mqtt dep from 2.18.8 to 3.0.0)
- Use AMQP durable option in assertExchange
- Use device apikey if exists in getEffectiveApiKey for command handling
